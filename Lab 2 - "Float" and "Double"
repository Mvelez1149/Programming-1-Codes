Lab 2 (Float and Double)
// This program will output the circumference and area
// of a circle with a given radius.
// Michael Fabian Velez and Albert Lozada

#include <iostream>
#include <string>
#include <cmath>

using namespace std;

// Problem 1.2 
// Definitions of problems for 1.2 to 1.4
const double PI = 3.14;
const double RADIUS = 5.4;
const double LENGTH = 8;
const double WIDTH = 3;

// Definitions of constants of problems for problem 1.5
const string FAVORITESODA = "Dr.Dolittle"; // use double quotes for strings
const char BESTRATING = 'A';

int main()
{
	// definitions for problem 1.2 
	float area; //  definition of area of circle 
	float circumference; // definition of circumference
	float perimeter;
		circumference = 2 * PI * RADIUS; // Computes circumference
	area = PI * RADIUS* RADIUS;
	cout << "The circumference is " << circumference << endl;
	cout << "the area of the circle is " << area << endl;
	cout << "\n\n";
	//Definition for problem 1.3 
	//Fill this with definition

	// Definition for problem 1.5
	char rating = 'B' ; //2nd highest product rating
	string favoriteSnack = "crackers"; // most preferred snack
	int numberOfPeople = 250; //the number of people in the survey
	int topChoiceTotal = 148; //the number of people who prefer the top choice
	//*** PROBLEM SOLUTION ****
	//Fill in the code for the cout statement that will output (with description)
	//the circumference
	//Fill in the code for the cout statement that will output (with description)
	// the area of the circle

	//Solution 1.3
	//Fill this with the solution for problem 1.3
	//Fill this with the solution for problem 1.3
	area = LENGTH * WIDTH;
		perimeter = 2 * (LENGTH * WIDTH);
		cout << " the area of the rectangle is " << area << endl;
		cout << " the perimeter of the rectangle is " << perimeter << endl;
		cout << "\n\n";

	//Solution 1.5
	//uncoment the following code and fill in the blanks of the following:
	cout << " the preferred soda is "<< FAVORITESODA << endl;
	cout << " the preferred snack is " << favoriteSnack << endl;
	cout << " out of " << numberOfPeople << " people " << 148 << " chose this items! " << endl;
	cout << " each of these products were given a rating of " << BESTRATING;
		cout << " from our experts tasters " << endl;
	    cout << " the other products were rated no higher than a " << rating << endl;
		
		system("pause");
return 0;
}
